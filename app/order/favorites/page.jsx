import FavoritesScreen from './FavoritesScreen';

export const metadata = { title: 'Saved Items', robots: { index: false, follow: false } };

export default function FavoritesPage() {
  return <FavoritesScreen />;
}
