import AddressScreen from './AddressScreen';

export const metadata = { title: 'Delivery Address', robots: { index: false, follow: false } };

export default function AddressPage() {
  return <AddressScreen />;
}
